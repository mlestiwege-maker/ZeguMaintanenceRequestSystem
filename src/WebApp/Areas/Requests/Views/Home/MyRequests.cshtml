@model ZEGU.WebApp.ViewModels.PagedResult<ZEGU.Core.Entities.Maintenance.MaintenanceRequest>

@{
    ViewData["Title"] = "My Requests";
}

<div class="page-header">
    <div>
        <h2><i class="bi bi-list-check"></i> My Maintenance Requests</h2>
        <p class="page-subtitle">Track and manage all your maintenance requests</p>
    </div>
    <a asp-area="Requests" asp-controller="Home" asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> New Request
    </a>
</div>

<div class="filter-bar">
    <form method="get" asp-action="MyRequests" class="row g-3">
        <div class="col-md-6">
            <label class="form-label small">Search</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-search"></i></span>
                <input type="text" name="search" class="form-control" placeholder="Search my requests..." value="@ViewBag.SearchTerm" />
            </div>
        </div>
        <div class="col-md-4">
            <label class="form-label small">Status</label>
            <select name="status" class="form-select">
                <option value="">All Statuses</option>
                @foreach (var status in ViewBag.Statuses)
                {
                    <option value="@status" selected="@(Context.Request.Query["status"] == status.ToString() ? "selected" : null)">@status</option>
                }
            </select>
        </div>
        <div class="col-md-2 d-flex align-items-end">
            <button type="submit" class="btn btn-primary w-100" title="Apply Filters">
                <i class="bi bi-funnel"></i> Filter
            </button>
        </div>
    </form>
</div>

@if (Model.Items.Any())
{
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover table-mobile-cards">
                    <thead>
                        <tr>
                            <th>Request #</th>
                            <th>Title</th>
                            <th class="hide-mobile">Category</th>
                            <th class="hide-mobile">Location</th>
                            <th>Priority</th>
                            <th>Status</th>
                            <th class="hide-mobile">Submitted</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var request in Model.Items)
                        {
                            <tr>
                                <td data-label="Request #"><strong>@request.RequestNumber</strong></td>
                                <td data-label="Title" class="text-truncate-2">@request.Title</td>
                                <td data-label="Category" class="hide-mobile">@request.Category.CategoryName</td>
                                <td data-label="Location" class="hide-mobile">@request.Location.Building!.BuildingName - @request.Location.RoomNumber</td>
                                <td data-label="Priority">
                                    <span class="badge priority-@request.Priority.ToString().ToLower()">@request.Priority</span>
                                </td>
                                <td data-label="Status">
                                    <span class="badge status-@request.Status.ToString().ToLower()">@request.Status</span>
                                </td>
                                <td data-label="Submitted" class="hide-mobile">@request.CreatedAt.ToString("dd/MM/yyyy")</td>
                                <td data-label="Actions" class="table-actions">
                                    <a asp-action="Details" asp-route-id="@request.Id" class="btn btn-sm btn-info" title="View Details">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    @if (request.Status == ZEGU.Core.Enums.MaintenanceRequestStatus.Submitted || request.Status == ZEGU.Core.Enums.MaintenanceRequestStatus.Assigned)
                                    {
                                        <form method="post" asp-action="Cancel" asp-route-requestId="@request.Id" style="display: inline;" onsubmit="return confirm('Are you sure you want to cancel this request?');">
                                            <button type="submit" class="btn btn-sm btn-danger" title="Cancel Request">
                                                <i class="bi bi-x-circle"></i>
                                            </button>
                                        </form>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    @await Html.PartialAsync("_Pagination", Model)
}
else
{
    var hasActiveFilter = !string.IsNullOrEmpty(Context.Request.Query["search"]) || !string.IsNullOrEmpty(Context.Request.Query["status"]);
    <div class="card">
        <div class="card-body">
            <div class="empty-state">
                <i class="bi bi-inbox"></i>
                @if (hasActiveFilter)
                {
                    <h4>No matching requests</h4>
                    <p>No requests match your search or filter. Try clearing them.</p>
                    <a asp-action="MyRequests" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Clear Filters
                    </a>
                }
                else
                {
                    <h4>No requests found</h4>
                    <p>You haven't submitted any maintenance requests yet.</p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> Submit Your First Request
                    </a>
                }
            </div>
        </div>
    </div>
}
