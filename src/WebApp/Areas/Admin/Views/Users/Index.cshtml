@model ZEGU.WebApp.ViewModels.PagedResult<ZEGU.Core.Entities.Identity.ApplicationUser>

@{
    ViewData["Title"] = "Manage Users";
}

<div class="page-header">
    <div>
        <h2><i class="bi bi-people"></i> Manage Users</h2>
        <p class="page-subtitle">Create and manage system users</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-person-plus"></i> Add New User
    </a>
</div>

<div class="filter-bar">
    <form method="get" asp-action="Index" class="row g-3">
        <div class="col-md-4">
            <label class="form-label small">Search</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-search"></i></span>
                <input type="text" name="search" class="form-control" placeholder="Search name, email, username..." value="@ViewBag.SearchTerm" />
            </div>
        </div>
        <div class="col-md-2 d-flex align-items-end">
            <button type="submit" class="btn btn-primary w-100" title="Apply Filter">
                <i class="bi bi-funnel"></i> Filter
            </button>
        </div>
    </form>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-mobile-cards">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th class="hide-mobile">Email</th>
                        <th class="hide-mobile">Username</th>
                        <th>Role</th>
                        <th class="hide-mobile">Department</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.Items)
                    {
                        <tr>
                            <td data-label="Name"><strong>@user.FirstName @user.LastName</strong></td>
                            <td data-label="Email" class="hide-mobile">@user.Email</td>
                            <td data-label="Username" class="hide-mobile">@user.UserName</td>
                            <td data-label="Role">@user.Role</td>
                            <td data-label="Department" class="hide-mobile">@user.Department?.DepartmentName</td>
                            <td data-label="Status">
                                @if (user.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">Inactive</span>
                                }
                            </td>
                            <td data-label="Actions" class="table-actions">
                                <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-info" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form method="post" asp-action="ToggleActive" asp-route-id="@user.Id" style="display: inline;">
                                    <button type="submit" class="btn btn-sm @(user.IsActive ? "btn-danger" : "btn-success")" title="@(user.IsActive ? "Deactivate" : "Activate")">
                                        <i class="bi bi-@(user.IsActive ? "x-circle" : "check-circle")"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@await Html.PartialAsync("_Pagination", Model)
